# SPX Gate 6F-B Implementation Plan

1. Add failing unit assertions for the injectable Create persistence seam and the exact Vietnamese `7001` label.
2. Make the minimal production changes and run the focused PHP 7.4 tests.
3. Add a resumable Gate 6F-B runtime harness with durable one-shot markers, strict sandbox allowlist, safe response audit, bounded Search, and all safety assertions.
4. Run all network-free lint/tests/security checks before any external request.
5. Run the integration harness once. It may issue exactly one Create and, only from canonical `1001`, exactly one Cancel; all recovery uses bounded Search.
6. Run HPOS sync/reload, UI/timeline/scheduler/idempotency checks, log/security scan, and the complete requested regression suite.
7. Review modified files and report exact command exit codes, sanitized network audit, actual terminal outcome, and limitations. Do not commit, push, or start Phase 6M.
